ASK 12000 0.1 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.1 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.1 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.2 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.2 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.2 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.3 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.3 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.3 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.4 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.4 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.4 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.5 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.5 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.5 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.6 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.6 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.6 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.7 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.7 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.7 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.8 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.8 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.8 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.9 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.9 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.9 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.0 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.0 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.0 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.1 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.1 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.1 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.2 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.2 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.2 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.3 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.3 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.3 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.4 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.4 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.4 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.5 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.5 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.5 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.6 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.6 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.6 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.7 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.7 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.7 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.8 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.8 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.8 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.9 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.9 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.9 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 2.0 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 2.0 4 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 2.0 4,4 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.1 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.1 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.1 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.2 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.2 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.2 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.3 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.3 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.3 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.4 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.4 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.4 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.5 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.5 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.5 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.6 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.6 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.6 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.7 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.7 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.7 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.8 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.8 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.8 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.9 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.9 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.9 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.0 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.0 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.0 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.1 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.1 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.1 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.2 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.2 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.2 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.3 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.3 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.3 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.4 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.4 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.4 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.5 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.5 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.5 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.6 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.6 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.6 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.7 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.7 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.7 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.8 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.8 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.8 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.9 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.9 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.9 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 2.0 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 2.0 8 1.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 2.0 8,8 1.0,2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.1 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.1 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.1 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.2 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.2 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.2 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.3 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.3 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.3 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.4 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.4 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.4 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.5 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.5 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.5 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.6 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.6 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.6 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.7 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.7 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.7 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.8 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.8 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.8 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.9 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.9 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.9 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.0 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.0 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.0 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.1 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.1 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.1 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.2 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.2 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.2 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.3 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.3 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.3 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.4 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.4 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.4 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.5 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.5 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.5 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.6 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.6 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.6 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.7 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.7 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.7 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.8 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.8 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.8 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.9 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.9 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.9 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 2.0 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 2.0 4 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 2.0 4,4 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.1 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.1 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.1 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.2 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.2 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.2 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.3 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.3 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.3 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.4 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.4 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.4 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.5 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.5 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.5 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.6 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.6 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.6 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.7 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.7 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.7 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.8 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.8 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.8 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 0.9 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 0.9 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 0.9 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.0 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.0 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.0 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.1 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.1 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.1 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.2 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.2 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.2 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.3 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.3 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.3 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.4 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.4 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.4 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.5 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.5 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.5 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.6 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.6 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.6 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.7 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.7 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.7 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.8 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.8 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.8 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 1.9 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 1.9 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 1.9 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

ASK 12000 2.0 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
PSK 12000 2.0 8 2.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*
APSK 12000 2.0 8,8 2.0,4.0 ; *modulation* *input_size* *signal_to_noise_ratio* *orders* *amplitudes*

